const GuideKeys = ({ text }) => {
  return (
    <div
      className="bg-iconstext rounded-sm px-1"
      style={{ width: "fit-content" }}
    >
      <p className="text-primary">{text}</p>
    </div>
  );
};

export default GuideKeys;
